<a href=
"<?php echo '/patients/' . $this->patient->getId() . '/edit';
?>"
>Edit</a>
<div class="cleared-container">
<?php
$patientData = $this->patient->toArray();
unset($patientData['id']);
unset($patientData['ownerId']);
foreach( $patientData as $k => $v) {
    echo '<b>' . $k . ':</b> ' . $v . '<br />';
}
?>
</div>