<a href=
"<?php echo '/patients/' . $this->patient->getId() . '/treatments';
?>"
>Treatments</a>

<div class="cleared-container">
<?php
$treatmentData = $this->treatment->toArray();
unset($treatmentData['id']);
unset($treatmentData['ownerId']);
foreach( $treatmentData as $k => $v) {
    echo '<b>' . $k . ':</b> ' . $v . '<br />';
}
?>
</div>